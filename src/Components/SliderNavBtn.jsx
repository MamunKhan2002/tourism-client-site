// import { useSwiper } from "swiper/react";

const SliderNavBtn = () => {
    // const swiper = useSwiper();
    return (
        <div className="slider_btn absolute top-0 z-50">
            hi
            {/* <button onClick={() => swiper.slidePrev()} className="border-2 border-red-400 ">prev</button>
            <button onClick={() => swiper.slideNext()} className="border-2 border-red-400 ">next</button> */}
        </div>
    );
};

export default SliderNavBtn;